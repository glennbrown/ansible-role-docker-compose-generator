volumes:
  db-data:
    driver: local

services:
  postgres:
    image: postgres:16
    container_name: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    restart: unless-stopped
  redis:
    image: redis:7
    container_name: redis
    restart: unless-stopped
