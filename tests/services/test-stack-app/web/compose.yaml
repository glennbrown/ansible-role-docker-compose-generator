networks:
  frontend:
    driver: bridge

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    networks:
      - frontend
    restart: unless-stopped
  certbot:
    image: certbot/certbot:latest
    container_name: certbot
    restart: unless-stopped
